# VFBiz Design Tokens

Build-time visual contract shared by Customer Portal, Workforce Portal and the
Keycloak Identity Theme. The package contains neutral VFBiz tokens only; it
does not claim approved VinFast branding.

```bash
npm run build
npm test
```
