# VFBiz Identity Theme

Native Keycloak login and email themes for the isolated Customer and Workforce
realms. Keycloak renders credentials directly; this application only builds a
versioned theme JAR.

```bash
npm run build
npm run verify
```

The build output is `dist/vfbiz-identity-theme-<version>-kc26.7.0.jar`.
