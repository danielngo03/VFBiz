import {ResourceTableSkeleton} from '@/components/feedback/resource-table-skeleton';

export function AuditPanelSkeleton() {
  return <ResourceTableSkeleton columns={6} label="Đang tải nhật ký kiểm toán" />;
}
