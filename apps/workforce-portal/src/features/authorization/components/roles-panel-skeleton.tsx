import {ResourceTableSkeleton} from '@/components/feedback/resource-table-skeleton';

export function RolesPanelSkeleton() {
  return <ResourceTableSkeleton columns={6} label="Đang tải danh sách role" />;
}
