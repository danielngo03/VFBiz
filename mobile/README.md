# Mobile workspace

Status: planning only; no React Native application is installed.

Planned ownership: the customer mobile experience, secure OIDC authorization-code
with PKCE, deep links, accessible native UI, scoped offline cache and generated
API client consumption.

Mobile never calls databases, Drupal entities or AI providers directly. Runtime
selection, navigation and native build structure begin with an approved mobile
foundation work item.
