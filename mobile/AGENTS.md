# Mobile workspace instructions

Read root `AGENTS.md` and this file. Read `README.md` for scope.

- Use the generated API contract when it exists; never hand-copy endpoint types.
- Store tokens only in approved secure native storage and isolate user cache.
- Small local visual changes may use fast lane; auth, deep-link, persistence,
  permission and dependency changes are controlled.
- Verify affected platforms and accessibility proportionally.
- Do not create mobile runtime code until an approved work item starts it.
